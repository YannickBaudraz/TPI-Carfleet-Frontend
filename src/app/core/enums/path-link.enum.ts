/**
 * Enum for carfleet backend api path links
 */
export enum PathLink {
  /**
   * Companies path links.
   */
  COMPANIES = 'companies',

  /**
   * Drivers path links.
   */
  DRIVERS = 'drivers',

  /**
   * Vehicles path links.
   */
  VEHICLES = 'vehicles'
}
